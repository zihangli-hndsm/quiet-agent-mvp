# 内容分类开发版验收（2026-09-12）

设备：Android 12，ZY22F65J2Z。APK SHA-256：`0B9DF53907725987B5E1329FBD68E1A9E12818E29715742C177898974580CFB6`。

已通过：
- 核心测试35项断言、安全41项、票据36项；新增用途路径、去重、模型来源标识、目录元数据兼容与越界拒绝测试。
- 真机 `content` 场景：先选目录再输入任务，模型返回后授权按钮可用；切换模式使准备状态失效。界面阶段不读取所选真实目录内容。
- 三个独立生成的虚构 OOXML 文件验证 DOCX/XLSX/PPTX 文本提取；邮箱、手机号遮罩及文件名不出现在上传载荷中。
- 真实 DeepSeek 调用将“按用途分类我这些简历”路由至文件整理；分类结果为技术岗位简历、产品岗位简历、设计岗位简历。一份损坏 DOCX 被标为待核对。
- 经过实际一次性授权和 TaskService 生成4项归档，ZIP 哈希一致，成功后临时快照删除，分类说明和脱敏审计保存在结果目录。
- 安装命令返回 Success。未上传用户真实文件或用户目录内容，云端测试仅使用虚构样例。

边界：系统选择器不能授权 Android 12 存储根目录；用户必须先选可授权的目录。未实现全盘搜索。内容提取限现代 Office 和 UTF-8 文本；PDF、旧式 Office、加密/损坏/无文本文件进入待核对。Office 只提取可读文本，不计算公式、不执行宏。目录失败的已确认代码缺陷是授权文件与要求为空的归档输出目录混用，现已分离；没有对 shixiseng 的真实内容进行云端分类验收。

运行：先执行 `scripts/build.ps1`、`scripts/build-tests.ps1`，安装两个 APK，再运行 `adb shell am instrument -w -e scenario content app.quietagent.test/app.quietagent.test.QuietInstrumentation`。该场景访问模型接口并仅发送自动生成的虚构样例摘要。
