# 两分钟统一工作区演示

一句话任务：**“读取我选入的虚构简历和票据样例，提取可核对字段，生成 CSV 到成果根，并把损坏文件列为失败原因。”**

演示流程：选择 `samples/unified` 目录并授予授权，递归列出资源，打开一个文本和一个 Office 预览，运行任务，展示 CSV/HTML 成果；随后修改文本、删除一个副本并从回收区恢复，最后撤销授权并刷新显示拒绝。

全部样例均为虚构或现有测试素材复用；该演示不使用真实个人资料，也不声称真机/API 已验证。
