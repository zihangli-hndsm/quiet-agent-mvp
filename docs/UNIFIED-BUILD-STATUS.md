# Unified build status

- `scripts/build.ps1`: passed on 2026-09-14 (Asia/Shanghai); APK SHA-256 `82F24D90CF7542F4A2EF15E3CE5B0A4B09CA2300A00258E36B3E08AC4A927CAA`.
- `scripts/test-unified-storage.ps1`: `WorkspaceStoreHostTests OK`; `ToolExecutorHostTests OK`; `ToolCallEnvelopeTests OK`.
- `scripts/test-workspace.ps1`: `WorkspaceTests OK`; core/security/receipt suites passed with 35/41/36 assertions; receipt package verifier passed 2 tests.
- Tested on host: local resource create/read/overwrite backup/restore/trash, traversal and stale root rejection, fingerprint conflict, copy/move, journal. ToolExecutorHostTests actually exercised report/stat/read/list/mkdir/write_table/zip/copy/move/trash plus unknown arguments/tool. hash/search/duplicates/rename are not claimed as executed.
- Passed on the Android 12 test phone: persisted SAF directory selection; unified UI launch; live model tool loop; recursive read-only inventory; DOCX/XLSX extraction; local Chinese OCR; exact `.csv` creation through the system provider; source-file hash preservation; and exclusion of the out-of-scope control folder. The live OCR fixture produced merchant `青禾便利店`, date `2024-03-01`, and total `18.80 元`.
- Passed on the Android 12 test phone: real directory names replace the legacy generic label; the home screen collapses grants into one summary card; the dedicated management screen lists each grant and shows a per-item removal confirmation. Duplicate persisted URIs were compacted from three historical entries to two unique grants without removing source files.
- Passed on the Android 12 test phone: results are collapsed into a named summary, then expanded in a dedicated screen ordered by modification time. Each result shows an exact timestamp, filename and size, with preview and recoverable deletion controls.
- Passed on the Android 12 test phone: CSV results render as a native table with a distinct header row, cell borders, alternating row colors, vertical scrolling and horizontal access to off-screen columns. The bounded parser covers quoted commas, escaped quotes and quoted line breaks.
- Passed on the Android 12 test phone: unified SMS import reads up to the latest 300 inbox messages regardless of age, offers select-all/cancel-select-all for eligible messages, and caps each preview at two lines. Non-selectable rows identify `含验证码` and/or `短信过长（超过2000字）`; the real inbox exposed a code row with the expected reason. An actually empty inbox shows an explicit explanation instead of a blank dialog.
- Implemented at the execution boundary: SMS `write_table` calls are buffered and committed once at `finish`, producing one timestamped aggregate CSV even when the model submits rows in several calls. The model prompt also requires reading the selected set before writing the aggregate table.
- Fixed the 80-call failure for large SMS sets: `read_documents` validates and reads up to 20 scoped short-text resources per call, capped at 800 characters per item and 20,000 characters per batch. Host tests cover a 20-item batch and rejection of 21 items; an 85-message task now needs about five read calls instead of at least 85.
- Fixed the corresponding multi-image failure: `receipt_batch` accepts 1–10 validated workspace image IDs, runs local OCR serially, and returns compact merchant/date/amount/review fields in one tool result. The model is instructed to batch receipt images, avoid duplicate OCR/receipt calls, and ignore SMS imports and prior results during a receipt-only task. Host checks cover tool advertisement and rejection above 10 images; real-image rerun remains a device check.
- Added a compact Codex-style execution trace under Current Task. Each call records only its ordinal, localized tool label, planning/running/success/failure state, and bounded count summary; no resource IDs, filenames, document text, OCR text, or model JSON are shown. Job storage retains the latest 12 events and the home card renders the latest 6.
- Fixed persisted single-file SAF grants losing their file/directory flag after reload. A selected receipt image now remains a single-file resource after app restart instead of being opened as a directory; the host regression suite recreates the store and lists the restored image. The phone previously showed alternating successful and failed workspace-list calls because directory grants worked while the single-file grant failed.
- Passed on host and Android 12 UI: imported SMS copies are a separately counted workspace collection. The dedicated SMS manager supports preview, per-item selection, select-all/cancel-select-all and recoverable batch removal; removing a copy makes it unavailable to later tasks without changing the system SMS inbox.
- Passed on host and Android 12 UI: recycle metadata includes source name/path/root, backup size and creation time; restored items leave the recycle list. Results and recycle screens expose multi-select, select-all/cancel-select-all and batch actions. The phone check only toggled selection and did not mutate the user's existing files.
- Still requires manual device verification: single-file isolation UI, completing an actual SMS copy import, permission revocation, destructive-operation recovery, cancellation/process interruption, and concurrent typing in another foreground app.
- Single-file SAF rename remains intentionally unsupported; select the parent directory to rename safely.
